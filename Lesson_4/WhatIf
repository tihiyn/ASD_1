1. Если стек пуст, то цикл не выполнится.
2. Если стек не пуст и количество элементов в стеке чётно, то в результате работы цикла стек будет опустошён.
3. Если стек не пуст и количество элементов в стеке нечётно, то сначала стек будет опустошён, а далее всё зависит от реализации метода pop():
    3.1. Если рассматривать реализацию метода pop() из текущего занятия, то метод pop() вернёт null и цикл завершится;
    3.2. Если рассматривать реализацию метода pop() в Java, то метод pop() выбросит исключение EmptyStackException и выполнение цикла будет прервано.

